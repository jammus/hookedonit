<h2 class="subhead">Top Songs <span>in genre</span></h2>
<ol class="items">
    {% for vote in votes %}
        <li>
            <span class="label">{{ vote['title'] }}</span>
        </li>
    {% endfor %}
</ol>
